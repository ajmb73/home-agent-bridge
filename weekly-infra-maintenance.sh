#!/bin/bash
# Weekly Infra Maintenance — creates kanban task on Sunday mornings
# This script is called by Hermes cron, not directly

# The kanban create command will run via the cron session
# This file is just a placeholder marker

set -euo pipefail
echo "Weekly infra maintenance template ready"
echo "Task created by cron job"
